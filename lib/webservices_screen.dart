import 'package:flutter/material.dart';

class WebServicesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Web Services'),
      ),
      body: Center(
        child: Text('Bienvenido a Web Services'),
      ),
    );
  }
}
