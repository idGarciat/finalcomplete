import 'package:flutter/material.dart';

class GraficosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gráficos'),
      ),
      body: Center(
        child: Text('Bienvenido a Gráficos'),
      ),
    );
  }
}
