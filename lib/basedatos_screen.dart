import 'package:flutter/material.dart';

class BaseDatosScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Base de Datos'),
      ),
      body: Center(
        child: Text('Bienvenido a Base de Datos'),
      ),
    );
  }
}
